{
  "name": "chain",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix project\"",
    "test": "npm run test --prefix backend && npm run test --prefix project"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
